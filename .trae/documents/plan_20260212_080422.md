I will disable the VS Code Core AI UI elements (icons and menus) while keeping the underlying commands functional for the API/Extension. I will also enhance the `ai-diff-extension-sample` to support interactive diff simulation with "Apply on Save" and file operations.

1.  **Disable VS Code Core AI UI**:
    *   Modify `src/vs/workbench/contrib/chat/browser/chatEditing/chatEditingEditorActions.ts` and `src/vs/workbench/contrib/chat/browser/chatEditing/chatEditingActions.ts`.
    *   Comment out or remove the `menu` registration properties from actions like `ToggleDiffAction`, `AcceptAction`, `RejectAction`, `ToggleExplanationWidgetAction`, etc. This hides them from the UI (Editor Title, Context Menu) but keeps the `id` registered so `vscode.commands.executeCommand` still works.

2.  **Optimize `ai-diff-extension-sample`**:
    *   **Interactive Diff Simulation**:
        *   **Context Menu**: Register `aiDiffSample.startSimulation` in the `editor/context` menu.
        *   **Apply on Save**: Add a `vscode.workspace.onDidSaveTextDocument` listener in `extension.ts`. When the temporary simulation file is saved, automatically trigger `applySimulation`.
        *   **Close Tab**: After applying, programmatically close the temporary editor tab.
        *   **File Operations**: Enhance `applySimulation` logic:
            *   If the original file didn't exist (simulating creation), use `WorkspaceEdit.createFile`.
            *   If the modified content is empty (simulating deletion), use `WorkspaceEdit.deleteFile`.
    *   **Diff Logic**: Ensure `computeMinimalEdits` handles empty strings correctly for file creation/deletion scenarios.

3.  **Verification**:
    *   Verify AI icons are gone from the editor title bar.
    *   Verify the extension can still toggle diffs via its own UI.
    *   Verify "Interactive Diff Simulation" works: Right-click file -> Start -> Edit -> Ctrl+S -> Changes applied to Chat Session -> Temp tab closes.

